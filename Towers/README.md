# Towers/ — Conductor Framework

Conductor = link between drum sound and number theory zeta. Framework to plug gap into.

- `Conductor.lean`: defines `conductor_gap = 1 - tail_26`, links to `core`
- Uses `norm_num` for positivity.
- Methodology: abstract conductor >0 implies spectral determinant positivity.

## Mathlib vs Not
NOT in mathlib: Poincaré conductor. In mathlib: `Summable` framework we use.

## Structure
Single file, imported by C05/C06/C10.
